{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<div class="row justify-content-center">
			<div class="col-md-8">
				<div class="card shadow-lg">
					<div class="card-header bg-primary text-white">
						<h4 class="mb-0 text-center">E-mail de réinitialisation du mot de passe envoyé</h4>
					</div>
					<div class="card-body p-4">
						<p class="lead">
							S'il existe un compte correspondant à votre adresse e-mail, un e-mail a été envoyé avec un 
														                            lien pour réinitialiser votre mot de passe.
														Ce lien
							<br>expirera dans
							<strong>{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</strong>.
						</p>
						<p>
							Si vous ne recevez pas d'e-mail, veuillez vérifier votre dossier spam ou
							<a href="{{ path('app_forgot_password_request') }}" class="text-primary">essayer à nouveau</a>.
						</p>
					</div>
					<div class="card-footer text-center">
						<a href="{{ path('app_home') }}" class="btn btn-outline-primary ">Retour</a>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
